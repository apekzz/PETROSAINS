# Copy this file to .env and edit if your Postgres login is different.
# Never commit .env — it can contain passwords.

DATABASE_URL=postgresql://oneshot:oneshot@127.0.0.1:5432/oneshot_inventory
# Optional override. Defaults to inventory_app/models/open_clip_pytorch_model.bin
# OPENCLIP_CHECKPOINT=C:\path\to\open_clip_pytorch_model.bin
